"""CLI entry points for the worker package.

The ``genesis-worker-ui`` console script in pyproject.toml resolves here.
Other CLI modules are thin wrappers around the facade; they are wired
into the Makefile only during Phase 10 retirement (post-v1, ADR-008).
"""
